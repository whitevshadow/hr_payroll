[pytest]
# Root config is primarily for the end-to-end test in tests/.
# Run the per-service unit tests from inside each service directory
# (see README) to avoid the duplicate top-level `app` package collision.
testpaths = tests
python_files = test_*.py
